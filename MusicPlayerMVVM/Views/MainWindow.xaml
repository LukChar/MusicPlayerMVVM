<Window x:Class="MusicPlayerMVVM.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:MusicPlayerMVVM.Views"
        Title="Music Player" 
        Height="750" Width="600"
        MinHeight="650" MinWidth="500"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterScreen"
        Background="#222222">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Resources/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ContentControl Grid.Row="0" Content="{Binding CurrentView, FallbackValue={x:Type views:HomeView}}" />

        <Border Grid.Row="1" Visibility="{Binding ControlBarVisibility}" Height="70" Background="#111111" BorderBrush="#333333" BorderThickness="0,1,0,0">
            <Grid Margin="15,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Button Command="{Binding GlobalPreviousCommand}" Content="⏮" Width="40" Height="40" Margin="0,0,15,0" Background="#1DB954" Foreground="White" BorderThickness="0" FontSize="18" Cursor="Hand"/>
                    <Button Command="{Binding GlobalPlayPauseCommand}" Content="⏯" Width="50" Height="50" Margin="0,0,15,0" Background="#1DB954" Foreground="White" BorderThickness="0" FontSize="20" Cursor="Hand"/>
                    <Button Command="{Binding GlobalNextCommand}" Content="⏭" Width="40" Height="40" Background="#1DB954" Foreground="White" BorderThickness="0" FontSize="18" Cursor="Hand"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <TextBlock Text="Lautstärke:" Foreground="White" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <Slider Width="120" 
                            Minimum="0" 
                            Maximum="1" 
                            Value="{Binding Volume, UpdateSourceTrigger=PropertyChanged}" 
                            VerticalAlignment="Center" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>